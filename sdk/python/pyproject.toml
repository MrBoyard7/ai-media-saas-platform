[project]
name = "ai-media-saas-sdk"
version = "1.0.0"
description = "Official Python SDK for the AI Media SaaS Platform"
requires-python = ">=3.9"
dependencies = ["httpx>=0.27"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
